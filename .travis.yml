language: python
python:
  - "3.3"
  - "3.4"
install:
  - pip install -U setuptools
  - pip install -U pip
  - pip install zc.buildout
  - pip install -e .[dev]
  - buildout
script:
  - ./bin/tangled test
  - ./bin/nosetests
