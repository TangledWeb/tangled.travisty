language: python
python:
  - "3.3"
  - "3.4"
install:
  - pip install -U setuptools
  - pip install -U pip
  - pip install zc.buildout
  - buildout dev:dependent-scripts=true
  - ls ./bin
script:
  - ./bin/tangled test
